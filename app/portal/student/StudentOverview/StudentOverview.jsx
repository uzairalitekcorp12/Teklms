"use client";
import {useMemo} from 'react';
import {ArrowRight,BookOpen,ClipboardCheck,UserCheck,Trophy,FileText,Bell} from 'lucide-react';
import StatCard from '../../_components/StatCard/StatCard';
import {courses,timetable} from '@/app/data/seedData';
import {useLmsStore} from '@/app/state/LmsStore';
import './StudentOverview.css';
export default function StudentOverview({onNavigate}){const{state}=useLmsStore();const today=timetable.Wed;const assigned=state.profile?.courseCodes||[];const myCourses=useMemo(()=>assigned.length?courses.filter(c=>assigned.includes(c.code)):courses,[assigned]);const firstName=(state.profile?.name||'Student').split(' ')[0];return <><section className="student-welcome"><div><span>{state.profile?.grade||'Student workspace'} · Wednesday, 9 September 2026</span><h1>Good afternoon, {firstName}</h1><p>Your classes, coursework, progress and important updates are organized here for the day ahead.</p><button onClick={()=>onNavigate('schedule')}>View today’s schedule <ArrowRight/></button></div><div className="term-progress"><div className="progress-ring"><b>78%</b><small>Term progress</small></div><span>Fall Term · Week 7 of 9</span></div></section><div className="student-overview-stats"><StatCard icon={BookOpen} label="Active Courses" value={myCourses.length} meta="Assigned this term" onClick={()=>onNavigate('courses')}/><StatCard icon={ClipboardCheck} label="Assignments" value={`${state.assignments.filter(a=>a.status!=='Pending').length} / ${state.assignments.length}`} meta="Completed or submitted" tone="green" onClick={()=>onNavigate('assignments')}/><StatCard icon={UserCheck} label="Attendance" value="94%" meta="Above requirement" tone="blue" onClick={()=>onNavigate('attendance')}/><StatCard icon={Trophy} label="Average Grade" value="A-" meta="83.2% overall" tone="orange" onClick={()=>onNavigate('results')}/></div><div className="student-dashboard-grid"><section className="student-card overview-courses"><SectionTitle title="My courses" action="View all" onClick={()=>onNavigate('courses')}/>{myCourses.slice(0,4).map(c=><button className="overview-course-row" key={c.code} onClick={()=>onNavigate('courses')}><span className={`overview-course-badge ${c.color}`}>{c.code.split('-')[0]}</span><div><b>{c.title}</b><small>{c.teacher}</small></div><div className="overview-course-progress"><span>{c.progress}%</span><i><em style={{width:`${c.progress}%`}}/></i></div><ArrowRight/></button>)}</section><section className="student-card today-schedule"><SectionTitle title="Today’s schedule" action="Full schedule" onClick={()=>onNavigate('schedule')}/><div className="today-list">{today.slice(0,4).map(([time,subject,room,teacher],i)=><button key={`${time}-${subject}`} onClick={()=>onNavigate('schedule')}><span>{time}</span><i className={`schedule-dot s-${i}`}/><div><b>{subject}</b><small>{room} · {teacher}</small></div></button>)}</div></section></div><div className="student-dashboard-grid lower"><section className="student-card"><SectionTitle title="Upcoming assignments" action="All assignments" onClick={()=>onNavigate('assignments')}/><div className="overview-assignment-list">{state.assignments.filter(a=>a.status==='Pending').slice(0,4).map(a=><button key={a.id} onClick={()=>onNavigate('assignments')}><span><FileText/></span><div><b>{a.title}</b><small>{a.subject} · Due {a.due}</small></div><em>{a.priority}</em></button>)}</div></section><section className="student-card"><SectionTitle title="Announcements" action="View all" onClick={()=>onNavigate('announcements')}/><div className="overview-announcements">{state.announcements.slice(0,4).map(a=><button key={a.id} onClick={()=>onNavigate('announcements')}><span><Bell/></span><div><b>{a.title}</b><small>{a.meta}</small></div>{!a.read&&<i/>}</button>)}</div></section></div></>}
function SectionTitle({title,action,onClick}){return <div className="student-section-title"><b>{title}</b>{action&&<button onClick={onClick}>{action}<ArrowRight/></button>}</div>}
